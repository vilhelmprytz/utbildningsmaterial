---
install:
  - curl -L -o hugo.deb https://github.com/gohugoio/hugo/releases/download/v0.63.0/hugo_extended_0.63.0_Linux-64bit.deb
  - sudo dpkg -i hugo.deb
  - hugo version
  - gem install mdl

script:
  - hugo --minify
  - mdl content --rules "~MD013"